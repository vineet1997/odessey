node_modules
dist
dist-ssr
*.local

# Vite/TS build info
*.tsbuildinfo

# Env files — never commit real secrets (e.g. the Google Maps key)
.env
.env.*
!.env.example

# Working outreach research can include private notes and contact details.
data/distribution/

# Editor/OS
.DS_Store

# Reference/working material — not part of the shipped app. images/ holds
# raw AI-generator outputs (~200MB) plus the official poster/teaser (real
# studio IP, shouldn't live in a public repo); demo video/ is a one-off
# screen recording used as a design reference. The production assets
# derived from these live in public/assets/ and ARE committed.
images/
demo video/
